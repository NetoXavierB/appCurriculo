import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    card:{
        width:'60%',
        borderRadius:5,
        borderWidth:1,
        borderColor: '#939393',
        alignSelf:'center',
        marginTop:20,
        padding:10,
        backgroundColor: '#fff'
    },
    card_header:{
        
    },
        card_content:{
        marginTop:20
    }
});

export default styles;